<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sist.user.todo.TodoMapper">
	<select id="selectTodoList" parameterType="todoRangeDTO"
		resultType="todoDomain">
		SELECT
		t.todo_no AS todoNo,
		t.title,
		t.content,
		t.status,
		t.start_date AS
		startDate,
		t.end_date AS endDate,
		u2.user_name AS userName,
		(
		SELECT
		LISTAGG(u.user_name, ', ')
		WITHIN GROUP (ORDER BY u.user_name)
		FROM
		representative tr
		JOIN users u
		ON tr.REPRESENTATIVE_NO = u.user_no
		WHERE
		tr.TODO_NO = t.todo_no
		) AS
		representativeUserNames
		FROM todo t
		LEFT JOIN
		users u2 ON t.user_no = u2.user_no
		<where>
			<!-- 1. '담당 할 일' (representativeUserNo만 넘어왔을 때) -->
			<if
				test="representativeUserNo != null and representativeUserNo != '' and (userNo == null or userNo == '')">
				t.todo_no IN (
				SELECT TODO_NO
				FROM REPRESENTATIVE
				WHERE
				REPRESENTATIVE_NO = #{representativeUserNo}
				)
			</if>

			<!-- 2. '요청 할 일' (내가 작성한 할 일, userNo만 넘어왔을 때) -->
			<if
				test="userNo != null and userNo != '' and (representativeUserNo == null or representativeUserNo == '')">
				t.user_no = #{userNo}
			</if>

			<!-- 3. '전체 할 일' (둘 다 넘어오거나 전체 조회를 원할 때) -->
			<if test="userNo != null and representativeUserNo != null">
				(
				t.user_no = #{userNo}
				OR
				t.todo_no IN (
				SELECT TODO_NO
				FROM
				REPRESENTATIVE <!-- 'todo_representative'에서 'REPRESENTATIVE'로 수정 -->
				WHERE REPRESENTATIVE_NO = #{representativeUserNo}
				)
				)
			</if>
			<if test="keyword != null and keyword != ''">
            AND t.title LIKE '%' || #{keyword} || '%'
        </if>
		</where>
	</select>
	<select id="selectrepresentativeList"
		parameterType="todoRangeDTO" resultType="todoDomain">
		select u.user_name userName
		from representative r join users u on u.user_no = r.representative_no
		where r.user_no = #{userNo}
	</select>

	<insert id="insertTodo" parameterType="todoDTO">
		<!-- 1. 시퀀스 숫자를 'td000000' 형태의 문자열로 가공해서 DTO의 todoNo 필드(String)에 저장 -->
		<selectKey keyProperty="todoNo" resultType="String"
			order="BEFORE">
			SELECT 'TD' || TO_CHAR(seq_todo_no.NEXTVAL, 'FM000000') FROM DUAL
		</selectKey>

		<!-- 2. 위에서 만든 #{todoNo}를 첫 번째 파라미터로 넣어서 INSERT -->
		insert into todo(todo_no, user_no, title, content, status, start_date,
		end_date, bookmark)
		values(
		#{todoNo},
		#{userNo},
		#{title},
		#{content},
		'0',
		systimestamp,
		<choose>
			<when test="endDate != null and endDate != ''">
				to_timestamp(#{endDate}, 'YYYY-MM-DD')
			</when>
			<otherwise>
				null
			</otherwise>
		</choose>
		,
		'0'
		)
	</insert>

	<!-- 담당자 매핑 테이블 (그대로 사용하시면 됩니다) -->
	<insert id="insertTodoRepresentative">
		insert into REPRESENTATIVE (representative_no, todo_no, user_no)
		values (#{representUserNo}, #{todoNo}, #{userNo})
	</insert>
	
	<update id="updateTodoStatus">
    UPDATE todo
    SET status = #{status}
    WHERE todo_no = #{todoNo}
</update>
<delete id="deleteTodoRepresentatives" parameterType="java.util.List">
    DELETE FROM REPRESENTATIVE
    WHERE TODO_NO IN
    <foreach collection="list" item="todoNo" open="(" separator="," close=")">
        #{todoNo}
    </foreach>
</delete>

<!-- 2. 할 일(todo) 데이터 삭제 -->
<delete id="deleteTodos" parameterType="java.util.List">
    DELETE FROM todo
    WHERE todo_no IN
    <foreach collection="list" item="todoNo" open="(" separator="," close=")">
        #{todoNo}
    </foreach>
</delete>
</mapper>