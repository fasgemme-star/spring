<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sist.notice.NoticeMapper">
  <select id="selectNotice" resultType="NoticeDomain">
	SELECT NOTICE_NO noticeNo, NOTICE_TITLE noticeTitle
	FROM NOTICE
	WHERE STATUS = '게시중'
  </select>
  <select id="selectNoticeDetail" parameterType="String" resultType="NoticeDomain">
	SELECT NOTICE_TITLE noticeTitle, CONTENT content, FILES files, CREATED_DATE createdDate
	FROM NOTICE
	WHERE STATUS = '게시중' AND NOTICE_NO = #{ noticeNo }
  </select>
</mapper>